name: Create Draft Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  draft-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write         # REQUIRED for OIDC
      attestations: write     # REQUIRED by attest-build-provenance v2
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: '0'
          fetch-tags: true

      - name: Set up Env
        uses: ./.github/actions/setup-env

      - name: Validate the release tag
        id: release-tag
        env:
          RELEASE_REF_TYPE: ${{ github.ref_type }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          python scripts/release_tools.py validate-tag "$RELEASE_TAG" \
            --require-existing \
            --require-head \
            --ref-type "$RELEASE_REF_TYPE"

      - name: Run version checks
        run: make check-version

      - name: Inspect any existing release
        id: existing-release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if gh release view "$RELEASE_TAG" > /dev/null 2>&1; then
            if [[ "$(gh release view "$RELEASE_TAG" --json isDraft --jq .isDraft)" != "true" ]]; then
              echo "Release for tag '$RELEASE_TAG' is already published."
              exit 1
            fi
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build library source and wheel artifacts
        uses: ./.github/actions/build-library
        with:
          library-name: ${{ env.PACKAGE_NAME }}
          python-version: ${{ env.MAIN_PYTHON_VERSION }}
          checkout: false

      - name: Create or repair the draft GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IS_PRERELEASE: ${{ steps.release-tag.outputs.prerelease }}
          RELEASE_EXISTS: ${{ steps.existing-release.outputs.exists }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          release_flags=(--draft)
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            release_flags+=(--prerelease)
          fi
          if [[ "$RELEASE_EXISTS" != "true" ]]; then
            gh release create "$RELEASE_TAG" \
              --title "Release $RELEASE_TAG" \
              --notes "See [CHANGELOG.md](./CHANGELOG.md)" \
              --verify-tag \
              "${release_flags[@]}"
          else
            echo "Reusing the existing draft for $RELEASE_TAG."
          fi
          gh release upload "$RELEASE_TAG" dist/* --clobber
